<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrueCallerMirror" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">#E62E2D2D</item>
    </style>
    <style name="TransparentActivityTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowSoftInputMode">adjustPan</item>
    </style>
    <style name="ThemeOverlay.Material3.TimePicker" parent="ThemeOverlay.Material3">
        <!-- Customize as needed -->
        <item name="android:height">200dp</item>
        <item name="android:layout_width">100dp</item>
    </style>
</resources>